{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "hireers",
  "main": "worker/index.ts",
  "compatibility_date": "2025-06-01",
  "assets": {
    "binding": "ASSETS",
    "not_found_handling": "single-page-application"
  },
  "vars": {
    // Firebase project ID (not a secret) — must match VITE_FIREBASE_PROJECT_ID
    "FIREBASE_PROJECT_ID": "jobs-9b284"
  },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "jobs-db",
      "database_id": "5ccdf67f-ec7f-4075-a5f2-b763ef8ddec0",
      "migrations_dir": "migrations"
    }
  ],
  "triggers": {
    // Smart scaling refresh — see targetIntervalHours()/smartSyncTick() in worker/index.ts.
    // Ticking every 30 min lets the scheduler stagger refreshes finely as more sources are added,
    // instead of refreshing every source on every tick.
    "crons": ["*/30 * * * *"]
  },
  "observability": {
    "enabled": true
  }
}
